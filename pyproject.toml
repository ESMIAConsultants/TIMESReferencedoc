[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "IEA-ETSAP MARKAL/TIMES list of references"
authors = [{name = "ESMIA", email = "info@esmia.ca"}]
dynamic = ["version", "description"]
